- provide(:title, "Все города")
%h1 Все города
/= @city.title
- @cities.each do |city|
  %h4
    = link_to city.title, city_path(city)
  %p
    /= raw truncate(city.content, :length => 500, :omission => "...")
    /= link_to "Читать далее ...", city_path(city)
  %hr